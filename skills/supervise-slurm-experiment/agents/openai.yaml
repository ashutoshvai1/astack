interface:
  display_name: "Supervise Slurm Experiment"
  short_description: "Supervise research jobs with passive heartbeats"
  default_prompt: "Use $supervise-slurm-experiment to monitor this project's experiment and continue authorized phases when its gates permit."
policy:
  allow_implicit_invocation: false
